version: '3.9'
services:
  db:
    image: postgres:16
    container_name: umkm_gebang_db
    environment:
      POSTGRES_USER: ${PGUSER}
      POSTGRES_PASSWORD: ${PGPASSWORD}
      POSTGRES_DB: ${PGDATABASE}
    ports:
      - "${PGPORT}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  adminer:
    image: adminer
    container_name: umkm_gebang_adminer
    ports:
      - "8010:8080"
    depends_on:
      - db

volumes:
  postgres-data: